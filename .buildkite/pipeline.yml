steps:
  - label: ":clojure:"
    command:
      - ".buildkite/script.sh"
    plugins:
      docker#v1.3.0:
        image: "clojure"
    branches: "feature/* release/* hotfix/*"

  - wait: ~
    continue_on_failure: true

  - label: "push logs"
    command: .buildkite/postlogs.sh "@denis.khoshaba"
