(ns stellar-ingest.core-test
  (:require [clojure.test :refer :all]
            [stellar-ingest.core :refer :all]))

(deftest a-test
  (testing "Just a dummy test"
    (is (= 1 1))))

(deftest another-test
  (testing "Just another dummy test"
    (is (> 2 1))))
